# Configuration de production pour Docker
APP_ENV=prod
APP_DEBUG=0

# Secret généré pour la production - CHANGEZ CETTE VALEUR !
APP_SECRET=CHANGE_ME_IN_PRODUCTION_9b6c4c8f2a7d8e1f3a5c9e2b8d6f4a1c

# Configuration de la base de données (sera surchargée par les variables d'environnement Docker)
DATABASE_URL="postgresql://app:!ChangeMe!@database:5432/app?serverVersion=16&charset=utf8"

# Configuration Messenger en production
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0

# Configuration du mailer - à adapter selon votre service
MAILER_DSN=null://null

# Configuration des logs
MONOLOG_LOG_LEVEL=error

# Configuration du cache - utilise le cache système
CACHE_DSN=filesystem

# Configuration des sessions
SESSION_LIFETIME=3600

# Optimisations de performance
OPCACHE_ENABLE=1
OPCACHE_VALIDATE_TIMESTAMPS=0

# Configuration de sécurité
TRUSTED_PROXIES=127.0.0.1,REMOTE_ADDR
TRUSTED_HOSTS=^(localhost|127\.0\.0\.1)$