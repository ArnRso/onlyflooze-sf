{% extends 'base.html.twig' %}

{% block title %}Transactions Récurrentes - OnlyFlooze{% endblock %}

{% block body %}
    <div class="container py-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="h2 text-primary">🔄 Transactions Récurrentes</h1>
            <div>
                <a href="{{ path('app_recurring_transaction_monthly_recap') }}" class="btn btn-info me-2">
                    <i class="fas fa-calendar-check"></i> Récap mensuel
                </a>
                <a href="{{ path('app_recurring_transaction_new') }}" class="btn btn-primary">
                    <i class="fas fa-plus"></i> Nouvelle transaction récurrente
                </a>
            </div>
        </div>

        {% if recurring_transactions|length > 0 %}
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">Liste des transactions récurrentes</h5>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-light">
                            <tr>
                                <th>Nom</th>
                                <th>Créée le</th>
                                <th>Transactions liées</th>
                                <th class="text-center">Actions</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for recurring_transaction in recurring_transactions %}
                                <tr>
                                    <td>
                                        <strong>{{ recurring_transaction.name }}</strong>
                                    </td>
                                    <td>
                                        <small
                                            class="text-muted">{{ recurring_transaction.createdAt|date('d/m/Y H:i') }}</small>
                                    </td>
                                    <td>
                                        <span class="badge bg-info">{{ recurring_transaction.transactions|length }} transaction(s)</span>
                                    </td>
                                    <td class="text-center">
                                        <div class="btn-group btn-group-sm">
                                            <a href="{{ path('app_recurring_transaction_show', {'id': recurring_transaction.id}) }}"
                                               class="btn btn-outline-info">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                            <a href="{{ path('app_recurring_transaction_edit', {'id': recurring_transaction.id}) }}"
                                               class="btn btn-outline-warning">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <button type="button" class="btn btn-outline-danger" data-bs-toggle="modal"
                                                    data-bs-target="#deleteModal{{ recurring_transaction.id }}">
                                                <i class="fas fa-trash"></i>
                                            </button>
                                        </div>

                                        <!-- Modal de suppression -->
                                        <div class="modal fade" id="deleteModal{{ recurring_transaction.id }}"
                                             tabindex="-1">
                                            <div class="modal-dialog">
                                                <div class="modal-content">
                                                    <div class="modal-header">
                                                        <h5 class="modal-title">Confirmer la suppression</h5>
                                                        <button type="button" class="btn-close"
                                                                data-bs-dismiss="modal"></button>
                                                    </div>
                                                    <div class="modal-body">
                                                        <p>Êtes-vous sûr de vouloir supprimer cette transaction
                                                            récurrente ?</p>
                                                        <p class="fw-bold">{{ recurring_transaction.name }}</p>
                                                        {% if recurring_transaction.transactions|length > 0 %}
                                                            <p class="text-warning">
                                                                <i class="fas fa-exclamation-triangle"></i>
                                                                Attention
                                                                : {{ recurring_transaction.transactions|length }}
                                                                transaction(s) sont liées à cette transaction
                                                                récurrente.
                                                            </p>
                                                        {% endif %}
                                                    </div>
                                                    <div class="modal-footer">
                                                        <button type="button" class="btn btn-secondary"
                                                                data-bs-dismiss="modal">Annuler
                                                        </button>
                                                        <form method="post"
                                                              action="{{ path('app_recurring_transaction_delete', {'id': recurring_transaction.id}) }}"
                                                              style="display: inline;">
                                                            <input type="hidden" name="_token"
                                                                   value="{{ csrf_token('delete' ~ recurring_transaction.id) }}">
                                                            <button type="submit" class="btn btn-danger">Supprimer
                                                            </button>
                                                        </form>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="text-center py-5">
                <div class="mb-4">
                    <i class="fas fa-sync-alt fs-1 text-muted"></i>
                </div>
                <h4 class="text-muted">Aucune transaction récurrente</h4>
                <p class="text-muted">Créez votre première transaction récurrente pour organiser vos finances !</p>
                <a href="{{ path('app_recurring_transaction_new') }}" class="btn btn-primary">
                    <i class="fas fa-plus"></i> Créer une transaction récurrente
                </a>
            </div>
        {% endif %}
    </div>
{% endblock %}
