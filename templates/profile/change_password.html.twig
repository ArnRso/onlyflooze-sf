{% extends 'base.html.twig' %}

{% block title %}Changer mon mot de passe - {{ parent() }}{% endblock %}

{% block stylesheets %}
    {{ importmap('app') }}
{% endblock %}

{% block body %}
    <div class="container py-4">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow">
                    <div class="card-header bg-primary text-white">
                        <h4 class="mb-0">🔑 Changer mon mot de passe</h4>
                    </div>
                    <div class="card-body">
                        {{ form_start(form) }}

                        <div class="mb-3">
                            {{ form_label(form.currentPassword) }}
                            {{ form_widget(form.currentPassword) }}
                            {{ form_errors(form.currentPassword) }}
                            <div class="form-text">Veuillez saisir votre mot de passe actuel pour confirmer votre
                                identité.
                            </div>
                        </div>

                        <div class="mb-3">
                            {{ form_label(form.newPassword.first) }}
                            {{ form_widget(form.newPassword.first) }}
                            {{ form_errors(form.newPassword.first) }}
                            <div class="form-text">Le mot de passe doit contenir au moins 6 caractères.</div>
                        </div>

                        <div class="mb-3">
                            {{ form_label(form.newPassword.second) }}
                            {{ form_widget(form.newPassword.second) }}
                            {{ form_errors(form.newPassword.second) }}
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a href="{{ path('app_profile') }}" class="btn btn-secondary me-md-2">
                                <i class="fas fa-times"></i> Annuler
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-key"></i> Changer le mot de passe
                            </button>
                        </div>

                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
